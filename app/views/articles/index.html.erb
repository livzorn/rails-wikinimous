<h1>Funky Articles</h1>
<%= link_to "Create a new article", new_path %>
<% @articles.each do |article| %>
  <h2>
    <%= link_to article.title, article_path(article.id) %></h2>
  <p><%= article.content %></p>
<% end %>
